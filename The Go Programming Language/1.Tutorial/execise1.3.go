package mainimport (	"fmt"	"os"	"strings"	"time")func main() {	var s, sep string	var time1, time2 int64	time1 = time.Now().UnixMicro()	for i := 0; i < len(os.Environ()); i++ {		s += sep + os.Environ()[i]		sep = " "	}	time2 = time.Now().UnixMicro()	//fmt.Println(s)	fmt.Println("inefficient version:", time2-time1)	time1 = time.Now().UnixMicro()	s = strings.Join(os.Environ(), " ")	//fmt.Println(s)	time2 = time.Now().UnixMicro()	fmt.Println("efficient version:", time2-time1)}